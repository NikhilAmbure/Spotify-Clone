/*
// ScrollWord
window.addEventListener("scroll", ()=>{

    var scrollPosition = window.scrollY;
    var word;

    if (scrollPosition < 500){
        word = "English";
    }

    document.getElementById("scrollWord").textContent = word;
});
*/

